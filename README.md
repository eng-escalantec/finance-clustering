ni README.md -ItemType File
'## Finance Clustering (IA)
Proyecto de clustering de acciones (KMeans/DBSCAN/HDBSCAN). Entorno: Python 3.11.' | Out-File -Encoding UTF8 README.md

git add .
git commit -m "init: estructura base, .gitignore, requirements y README"
